import { View, Text, Button } from "react-native";

function SOS(props) {
  //   function goToContact() {
  //     props.navigation.navigate("Contact", { name: "John Doe" });
  //   }

  return (
    <View style={{ flex: 1 }}>
      <Text>SOS</Text>
      {/* <Button title="Go to Contact" onPress={goToContact} /> */}
    </View>
  );
}

export default SOS;
